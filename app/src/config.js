// ---------------------------------------------------------------------------
// Server connection config
// ---------------------------------------------------------------------------
// For local development, use your computer's LAN IP (run `hostname -I` on Ubuntu).
// For production via Cloudflare Tunnel, use your tunnel domain.
//
// Examples:
//   ws://192.168.1.42:8080      (local dev)
//   wss://home.yourdomain.com   (production via Cloudflare Tunnel)
// ---------------------------------------------------------------------------

export const SERVER_URL = "wss://home.mohitdahal.com";
